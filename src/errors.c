#include <stdio.h>
#include "errors.h"

void print_error(ErrorType error, char* additional_message){
    
    
    switch (error)
    {
    case MALLOC_ERROR:
        /* code */

        break;
    
    default:
        
        break;
    }
}